import React from "react";

const AuthForm = ({ type }: { type: string }) => {
  return (
    <div className="card-border lg:min-w-[566px]">
      <div className="card">Asif</div>
    </div>
  );
};

export default AuthForm;
